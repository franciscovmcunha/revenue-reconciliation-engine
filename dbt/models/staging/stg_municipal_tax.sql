-- Types and renames bronze.municipal_tax rows (Document Intelligence output,
-- period rollups) onto the canonical field set.
-- TODO: implement — select from {{ source('bronze', 'municipal_tax') }}

select 1 as placeholder
